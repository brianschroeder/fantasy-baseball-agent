The user wants to understand positional scarcity in the context of their current roster. No arguments needed.

1. Call `positional_scarcity` to get the league-wide remaining player counts
2. Call `my_roster` to see which positions are already filled

Then give a personalized scarcity assessment:
- For each position, show urgency level AND whether it's already filled on your roster
- Highlight the "danger zone" — unfilled positions with HIGH or fading MEDIUM urgency
- Name the top 1-2 remaining players at each scarce position so the user knows exactly who to target
- Give a bottom line: "Your most urgent unfilled position is [X] — [Player] is the last good option"

Format as a quick reference table + one-paragraph action summary.
